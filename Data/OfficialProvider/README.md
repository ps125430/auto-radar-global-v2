# Official Provider Fixtures

Status: Shadow Runtime

此目錄保存可重播的官方優先來源清冊與 Snapshot 輸入。檔案不得包含
Runtime 可直接存取的網址或 API endpoint。

- `provider_registry.json`: 來源身分、信任、更新頻率與健康狀態。
- `snapshot_input_2026-07-01-AM.json`: Living Ocean 第一份封存輸入。

所有輸出只允許寫入 `Runtime/Artifacts/OfficialData/`。Repository 維持唯讀。
