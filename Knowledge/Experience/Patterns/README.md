# Experience Patterns

status: schema_candidate
model_impact: research_only_not_production

Patterns store repeated market behaviors observed across cases.

Allowed pattern status values:

```text
pattern_candidate
pattern_validation
pattern_verified
pattern_deprecated
```

Patterns do not directly change Decision or production scoring.
